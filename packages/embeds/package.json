{
  "name": "@revinel/embeds",
  "version": "0.6.0",
  "private": false,
  "description": "Shared Revinel embed protocol — the postMessage contract between the embed iframe and its host wrappers.",
  "keywords": [
    "ads",
    "advertising",
    "embed",
    "postmessage",
    "protocol",
    "revinel"
  ],
  "homepage": "https://github.com/revinel/revinel/tree/main/packages/embeds#readme",
  "license": "MIT",
  "author": "Piotr Kulpinski",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/revinel/revinel.git",
    "directory": "packages/embeds"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "sideEffects": false,
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "publishConfig": {
    "access": "public",
    "directory": "dist"
  },
  "scripts": {
    "build": "tsdown && bun run prepare-package",
    "check-types": "tsc --noEmit --project tsconfig.json",
    "prepare-package": "bun ../../scripts/prepare-package.ts",
    "pub:release": "bun run build && npm publish ./dist --access public",
    "pub:beta": "bun run build && npm publish ./dist --access public --tag beta --no-git-checks",
    "pub:next": "bun run build && npm publish ./dist --access public --tag next --no-git-checks"
  },
  "dependencies": {},
  "devDependencies": {
    "@revinel/tsconfig": "*",
    "typescript": "^6.0.3"
  }
}
